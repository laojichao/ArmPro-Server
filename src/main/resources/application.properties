# Spring Boot HTTP Server Configuration
server.port=8080

# Disable Spring Boot banner
spring.main.banner-mode=off

# Disable Spring Boot logging (use log4j instead)
logging.level.root=WARN
logging.level.org.springframework=WARN
